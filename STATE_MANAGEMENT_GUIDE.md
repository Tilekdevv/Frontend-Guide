# 📊 Props vs Context API vs Redux Toolkit - Документация

## 🎯 Обзор страницы

Образовательная страница с визуальным сравнением трёх способов управления состоянием в React. Акцент на понятные объяснения и визуальные диаграммы.

---

## 📚 Структура страницы

### 1️⃣ Интерактивные табы (Props / Context / Redux)

**Функционал:**

- 3 кнопки для переключения между разделами
- Активная кнопка подсвечивается градиентом
- Плавная анимация смены контента

**Контент каждого таба:**

#### 🟢 Props Tab

- **Определение:** Простая передача данных родитель → ребёнок
- **Диаграмма:** Визуальная схема передачи данных
- **Props Drilling:** Объяснение проблемы с примером
- **Плюсы/Минусы:** Зелёные/красные иконки

#### 🔵 Context API Tab

- **Определение:** Глобальное состояние без props drilling
- **Диаграмма:** Provider → Multiple Components
- **Где использовать:** 4 реальных примера (темы, язык, auth, настройки)
- **Плюсы/Минусы:** Встроен в React / Может вызвать ре-рендеры

#### 🟣 Redux Toolkit Tab

- **Определение:** Мощная библиотека для больших проектов
- **Диаграмма:** Component → Reducer → Store (цикл)
- **Для больших проектов:** 4 причины использовать
- **Плюсы/Минусы:** Мощный, DevTools / Много boilerplate

---

### 2️⃣ Сравнительная таблица

**6 характеристик для сравнения:**

| Характеристика         | Props               | Context API         | Redux Toolkit    |
| ---------------------- | ------------------- | ------------------- | ---------------- |
| **Сложность**          | 🟢 Очень просто     | 🟡 Средне           | 🔴 Сложно        |
| **Настройка**          | 🟢 Не требуется     | 🟡 Минимальная      | 🔴 Много кода    |
| **Размер проекта**     | 🟢 Маленький        | 🟡 Средний          | 🟢 Большой       |
| **Производительность** | 🟢 Отлично          | 🟡 Хорошо           | 🟢 Отлично       |
| **Debugging**          | 🟢 Просто           | 🟡 Средне           | 🟢 DevTools      |
| **Когда использовать** | 🔵 Локальные данные | 🔵 Темы, язык, auth | 🔵 Сложный state |

**UI особенности:**

- Цветные бейджи для каждого значения
- Hover эффект на строках
- Responsive таблица с горизонтальным скроллом
- Анимация появления строк

---

### 3️⃣ Когда что использовать?

**3 карточки с use cases:**

#### 🟢 Props

- Передача данных родитель → ребёнок
- Маленькие компоненты
- Локальные данные
- Простые проекты

#### 🔵 Context API

- Темы (dark/light mode)
- Язык интерфейса
- Данные пользователя
- Средние проекты

#### 🟣 Redux Toolkit

- Большие приложения
- Сложная логика state
- Много компонентов
- Нужен debugging

**UI:**

- Grid 3 колонки (responsive)
- Градиентные иконки
- Списки с CheckCircle
- Hover анимация (подъём)

---

### 4️⃣ Реальные примеры кода

**3 code examples:**

#### 1. Props - Передача данных

```jsx
// Parent Component
function App() {
  const user = { name: "Иван", age: 25 };
  return <UserCard user={user} />;
}

// Child Component
function UserCard({ user }) {
  return (
    <div>
      <h2>{user.name}</h2>
      <p>Возраст: {user.age}</p>
    </div>
  );
}
```

#### 2. Context API - Global State

```jsx
// ThemeContext.js
const ThemeContext = createContext();

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState("dark");

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

// Использование
function Header() {
  const { theme, setTheme } = useContext(ThemeContext);

  return (
    <button onClick={() => setTheme(theme === "dark" ? "light" : "dark")}>
      Сменить тему
    </button>
  );
}
```

#### 3. Redux Toolkit - Store

```jsx
// cartSlice.js
import { createSlice } from "@reduxjs/toolkit";

const cartSlice = createSlice({
  name: "cart",
  initialState: { items: [] },
  reducers: {
    addItem: (state, action) => {
      state.items.push(action.payload);
    },
    removeItem: (state, action) => {
      state.items = state.items.filter((item) => item.id !== action.payload);
    },
  },
});

export const { addItem, removeItem } = cartSlice.actions;
export default cartSlice.reducer;

// Использование
function Cart() {
  const items = useSelector((state) => state.cart.items);
  const dispatch = useDispatch();

  return (
    <button onClick={() => dispatch(addItem({ id: 1, name: "Товар" }))}>
      Добавить в корзину
    </button>
  );
}
```

**UI компонента:**

- Заголовок + описание
- Code block с syntax highlighting
- Dark background для кода
- Overflow-x для длинных строк

---

### 5️⃣ Главный совет (Final CTA)

**Контент:**

> Начинайте с Props. Если появляется props drilling — используйте Context API.
> Если проект растёт и state становится сложным — переходите на Redux Toolkit.

> Не используйте Redux просто потому что "так принято". Выбирайте инструмент под задачу!

**UI:**

- Большая карточка с glassmorphism
- Эмодзи 💡
- Центрированный текст
- Italic для дополнительного совета

---

## 🎨 UI Компоненты

### PropsSection

- Иконка Box (зелёный градиент)
- Диаграмма: Parent → Child
- Props Drilling проблема (красная карточка)
- Плюсы/Минусы

### ContextSection

- Иконка Network (синий градиент)
- Диаграмма: Provider → 3 Components
- Use cases список
- Плюсы/Минусы

### ReduxSection

- Иконка Database (фиолетовый градиент)
- Диаграмма: Component → Reducer → Store
- Для больших проектов список
- Плюсы/Минусы

### Section

- Градиентная иконка
- Заголовок секции
- Анимация появления при скролле

### UseCaseCard

- Градиентная иконка
- Заголовок
- Список use cases с CheckCircle
- Hover эффект (подъём)

### CodeExample

- Заголовок + описание
- Code block с pre/code
- Dark background
- Анимация появления

---

## 🎨 Визуальные диаграммы

### Props Diagram

```
┌─────────────────────┐
│  Parent Component   │ (зелёная рамка)
└──────────┬──────────┘
           ↓ (зелёная стрелка)
┌─────────────────────┐
│  Child Component    │ (светло-зелёная рамка)
└─────────────────────┘
```

### Context API Diagram

```
┌─────────────────────┐
│  Context Provider   │ (синяя рамка)
└──────────┬──────────┘
     ↓     ↓     ↓ (3 синие стрелки)
┌────┐  ┌────┐  ┌────┐
│ A  │  │ B  │  │ C  │ (3 компонента)
└────┘  └────┘  └────┘
```

### Redux Toolkit Diagram

```
┌───────────┐   ┌─────────┐   ┌───────┐
│ Component │ → │ Reducer │ → │ Store │
└───────────┘   └─────────┘   └───────┘
      ↑                             │
      └─────────────────────────────┘
           (цикл обновления)
```

---

## 🎯 Образовательные цели

После прочтения студент должен понимать:

✅ Что такое Props и как они работают  
✅ Проблему Props Drilling  
✅ Что такое Context API и когда его использовать  
✅ Что такое Redux Toolkit и для чего он нужен  
✅ Разницу между тремя подходами  
✅ Когда какой инструмент выбирать  
✅ Реальные примеры использования

---

## 💡 Ключевые сообщения

### 1. Начинайте с простого

- Props — самый простой способ
- Не усложняйте без необходимости
- Большинство задач решается Props

### 2. Context для глобальных данных

- Темы, язык, auth — идеально для Context
- Избегает props drilling
- Встроен в React

### 3. Redux для сложных проектов

- Большие приложения
- Сложная логика state
- Нужен debugging и DevTools

### 4. Выбирайте инструмент под задачу

- Не используйте Redux везде
- Оценивайте сложность проекта
- Можно комбинировать подходы

---

## ✨ Интерактивные элементы

1. **Табы** - переключение между Props/Context/Redux
2. **Hover эффекты** - на всех карточках и строках таблицы
3. **Анимации** - fade in при скролле
4. **Code blocks** - с syntax highlighting

---

## 📱 Адаптивность

- **Desktop:** Таблица полная, grid 3 колонки
- **Tablet:** Таблица со скроллом, grid 2 колонки
- **Mobile:** Таблица со скроллом, grid 1 колонка

---

## 🎨 Цветовая схема

- **Зелёный** (Props) - простота, базовый уровень
- **Синий** (Context API) - средний уровень
- **Фиолетовый** (Redux) - продвинутый уровень
- **Красный** - предупреждения, минусы
- **Жёлтый** - средние значения

---

## 🚀 Уникальные фичи

1. **Визуальные диаграммы** - понятные схемы работы
2. **Интерактивные табы** - переключение между подходами
3. **Сравнительная таблица** - всё в одном месте
4. **Реальные примеры кода** - готовые к использованию
5. **Цветовое кодирование** - быстрое понимание

---

## 📊 Структура принятия решения

```
Нужно передать данные?
  ↓
Только родитель → ребёнок?
  ↓ ДА
  → Используйте Props

  ↓ НЕТ
Много уровней вложенности?
  ↓ ДА
  → Context API или Redux

Сложная логика state?
  ↓ ДА
  → Redux Toolkit

  ↓ НЕТ
  → Context API
```

---

Страница готова к использованию! 🎉
